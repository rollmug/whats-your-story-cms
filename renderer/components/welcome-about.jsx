import React from 'react';
import { Typography } from "@material-tailwind/react";

const WelcomeAbout = () => {
    return (
        <Typography variant="lead">
            Welcome! This installer app will get your Content Management System (CMS), database, and API service up and running.
        </Typography>
    );
}

export default WelcomeAbout;